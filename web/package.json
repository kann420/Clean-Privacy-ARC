{
  "name": "clean-privacy-arc-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Clean Privacy for Arc web application (Arc Testnet 5042002)",
  "scripts": {
    "generate-config": "node tools/generate-chain-config.mjs",
    "dev": "npm run generate-config && vite",
    "build": "npm run generate-config && tsc --noEmit && vite build",
    "build:deploy": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "extract-assets": "node tools/extract-mock-assets.mjs"
  },
  "dependencies": {
    "@rainbow-me/rainbowkit": "2.2.11",
    "@tanstack/react-query": "5.59.0",
    "@unlink-xyz/sdk": "0.3.0-canary.767",
    "qr": "0.6.0",
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "viem": "2.55.10",
    "wagmi": "2.19.5"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^6.0.3",
    "vitest": "^4.1.10"
  },
  "overrides": {
    "cuer": {
      "qr": "0.5.5"
    }
  }
}
